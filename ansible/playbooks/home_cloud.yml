# Run this to monitor build

- hosts: all
  gather_facts: false
  roles:
    - role: monitors

# - hosts: all
#   gather_facts: false
#   vars:
#     vm_build_state: "{{ global_vm_build_state | default('skipped') }}"
#   roles:
#     - helpers

# ---
# - hosts: all
#   gather_facts: false
#   roles:
#     - role: ipa_dns
