apiVersion: v1
kind: Service
metadata:
  name: dispatcher-dashboard-service
spec:
  selector:
    app: dispatcher-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
  type: ClusterIP
