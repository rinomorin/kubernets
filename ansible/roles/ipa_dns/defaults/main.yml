---
ipa_dns_zone: "{{ cloud_zone }}"

ipa_admin_user: "admin"
ipa_admin_password: "{{ lookup('env', 'ipa_admin_password') }}"

# API and ingress endpoints
ipa_api_record: "api"
ipa_apps_record: "*.apps"

# Whether workers get A records
ipa_register_workers: false

