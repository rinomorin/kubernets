- name: Wait for master nodes to register
  become: false
  wait_for:
    timeout: 60
# - name: Wait for master nodes to register
#   become: true
#   delegate_to: "{{ helper_host }}"
#   vars:
#     expected_masters: "{{ groups['control_nodes'] }}"
#   retries: 60
#   delay: 10
#   until: >
#     (
#       lookup('file', '/var/lib/cloud-helper/node-registry.json')
#       | from_json
#     ).keys() | intersect(expected_masters) | length
#     == expected_masters | length
